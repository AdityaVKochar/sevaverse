<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://fonts.googleapis.com/css?family=Lora"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css?family=Poppins"
      rel="stylesheet"
    />
    <title>Task Details - SevaVerse</title>
    <style>
      body {
        margin: 0;
        font-family: "Poppins", sans-serif;
        background-color: #f8f9fa;
      }
      .navbar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        background-color: #fb2f2f;
        padding: 20px;
        color: white;
      }
      .menu-btn,
      .profile-btn {
        cursor: pointer;
      }
      .container {
        max-width: 800px;
        margin: 50px auto;
        background: white;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      }
      h1 {
        text-align: center;
        color: #b71c1c;
        margin-bottom: 20px;
      }
      .task-details {
        font-size: 16px;
        line-height: 1.8;
      }
      .task-details p {
        margin: 10px 0;
      }
      .task-details strong {
        color: #333;
      }
      .btn-register {
        display: block;
        margin: 20px auto;
        padding: 10px 20px;
        background-color: #28a745;
        color: white;
        text-align: center;
        text-decoration: none;
        border-radius: 5px;
        font-size: 16px;
        font-weight: bold;
        width: fit-content;
      }
      .btn-register:hover {
        background-color: #218838;
      }
      .btn-back {
        display: block;
        margin: 20px auto;
        padding: 10px 20px;
        background-color: #b71c1c;
        color: white;
        text-align: center;
        text-decoration: none;
        border-radius: 5px;
        font-size: 16px;
        font-weight: bold;
        width: fit-content;
      }
      .btn-back:hover {
        background-color: #a61b1b;
      }
    </style>
  </head>
  <body>
    <div class="navbar">
      <span
        class="menu-btn"
        onclick="openSidebar()"
        style="font-size: 30px; color: black"
        >☰</span
      >
      <img src="/images/full_logo_black.png" width="267" height="61" />
      <img src="/images/profile.png" width="35" height="35" />
    </div>

    <div class="container">
      <h1>Task Details</h1>
      <div class="task-details">
        <p><strong>Title:</strong> <%= task.title %></p>
        <p><strong>Description:</strong> <%= task.description %></p>
        <p><strong>Location:</strong> <%= task.location %></p>
        <p>
          <strong>Start Time:</strong> <%= new
          Date(task.fromTime).toLocaleString() %>
        </p>
        <p>
          <strong>End Time:</strong> <%= new Date(task.toTime).toLocaleString()
          %>
        </p>
        <p><strong>Volunteers Needed:</strong> <%= task.size %></p>
        <p><strong>Organization:</strong> <%= task.organization.name %></p>
      </div>
      <a href="/volunteer/task/<%= task.id %>/register" class="btn-register"
        >Register for Task</a
      >
      <a href="/volunteer/dashboard" class="btn-back">← Back to Dashboard</a>
    </div>
  </body>
</html>
